{
  "name": "mgnovenie",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "lint": "npm --prefix apps/web run lint:all",
    "test": "npm --prefix apps/web run test",
    "lint-staged": "lint-staged"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2"
  }
}
