# MGNOVENIE web (`apps/web`)

Nuxt **4.5** storefront (Vue 3 + TypeScript). Source lives under `app/` (Nuxt 4 layout). Config: `nuxt.config.ts` (`compatibilityDate` pinned).

See the [repository README](../../README.md) for local setup, lint, Docker, and deploy.
