# Local-only overlay: publish Postgres to the host for psql / `npm run dev`.
# Do not use on the VPS.
#
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d db

services:
  db:
    ports:
      - "127.0.0.1:5432:5432"
